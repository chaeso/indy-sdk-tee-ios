# Uncomment the next line to define a global platform for your project
platform :ios, '13.4'

target 'ios-tester' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-tester
  pod 'libsodium'
  #pod 'libzmq',"4.3.2"
  pod 'OpenSSL'

  target 'ios-testerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios-testerUITests' do
    # Pods for testing
  end

end
